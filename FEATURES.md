# Features — Snapchat patches for Morphe

Pinned target: Snapchat `14.20.0.50` (see `PINNED_VERSION`, package `com.snapchat.android`).
One row per feature. Statuses: `todo` / `wip` / `done` (tested on device) / `blocked` (say why) / `skipped` (say why).

| Feature | Tier | Status | Patch name | Notes |
|---------|------|--------|-----------|-------|
| _(none yet — first patch TBD after version pin)_ | A | todo | — | — |
